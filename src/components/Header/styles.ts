import { styled } from '../../styles'

export const Container = styled('header', {
  width: '100%',
  maxWidth: 1180,
  margin: '0 auto',

  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',

  button: {
    marginLeft: 'auto',
  },
})
